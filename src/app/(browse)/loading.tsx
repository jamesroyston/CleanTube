/**
 * Browse route transitions use the top progress bar + client SWR cache.
 * Avoid a full-page skeleton on every navigation (e.g. returning to For You).
 */
export default function BrowseLoading() {
  return null;
}
