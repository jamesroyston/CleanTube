/** Expanded library channel rail width (px). */
export const CHANNELS_DRAWER_WIDTH = 276;

/** Collapsed mini-rail width aligned with fixed AppBar `left` / main `marginLeft`. */
export const CHANNELS_COLLAPSED_DRAWER_WIDTH = 77;
